all:
  vars:
    cluster_name: test
    kube_version: v1.14.1
    # TODO:
    # cluster_ca_cert: /tmp/ca.crt
    # cluster_ca_key: /tmp/ca.key
    kube_apiservice_external_fqdn: "f30-api-server.lab.lex"
    kube_services_subnet: "10.13.0.0/24"
    kube_cluster_cidr: "10.200.0.0/16"
  hosts:
    localhost:
  children:
    etcd:
      hosts:
        f30-master1.lab.lex:
    masters:
      hosts:
        f30-master1.lab.lex:
    workers:
      hosts:
        f30-worker1.lab.lex:
